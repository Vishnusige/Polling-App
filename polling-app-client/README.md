# Polling App Client

React frontend for the Polling App.

## Scripts

```bash
npm install
npm start
npm run build
```

The development server runs on `http://localhost:3000` and calls the backend through the relative `/api` path. In production, the Docker build copies the compiled React assets into the Spring Boot static resources directory so the frontend and API are served from the same origin.
